# Example noise simulation configuration for gwsim_noise (YAML alternative)
# Primary examples use TOML; this file is kept for users who prefer YAML.

detectors:
    - H1
    - L1
duration: 4.0
sampling_frequency: 4096.0
output:
    directory: ./output
    prefix: noise
seed: 42
